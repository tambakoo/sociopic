<%= form_for(current_user.active_relationships.build) do |i| %>
<% debugger %>
  <div><%= hidden_field_tag :followed_id, @user.id %></div>
  <%= i.submit "follow", class: "btn btn-primary" %>
<% end %>